<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.triviaapp">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.history.HistoryActivity"/>
        <activity android:name=".baseui.BaseActivity" />
        <activity android:name=".feature.summary.SummaryActivity" />
        <activity android:name=".feature.thirdpage.ThirdPageActivity" />
        <activity android:name=".feature.secondpage.SecondPageActivity" />
        <activity android:name=".feature.firstpage.FirstPageActivity" />
        <activity android:name=".feature.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>